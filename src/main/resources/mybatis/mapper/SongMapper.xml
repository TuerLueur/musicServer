<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.music.server.mapper.SongMapper">

    <resultMap id="songMap" type="com.music.server.model.Song">
        <result column="sid" jdbcType="BIGINT" property="sid"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="aid" jdbcType="BIGINT" property="aid"/>
        <result column="comments" jdbcType="VARCHAR" property="comments"/>
        <result column="pic" jdbcType="VARCHAR" property="pic"/>
    </resultMap>

    <sql id="songColumn">
       s.sid, s.name, s.aid
    </sql>

    <select id="searchSongByName" resultMap="songMap" parameterType="String">
        SELECT <include refid="songColumn"></include>
        FROM song s
        WHERE s.name LIKE "%"#{name}"%"
    </select>

    <select id="getCommentsById" resultType="String" parameterType="java.math.BigInteger">
        SELECT s.comments
        FROM song s
        WHERE s.sid = #{sid}
    </select>
</mapper>